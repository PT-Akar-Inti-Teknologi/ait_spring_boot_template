databaseChangeLog:
  - changeSet:
      id: add_fields_credential_table_user
      author: ait
      changes:
        - addColumn:
            tableName: user_ait
            columns:
              - column:
                  name: username
                  type: varchar(255)
              - column:
                  name: password
                  type: varchar(255)